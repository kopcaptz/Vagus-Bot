# 🔄 Перезапуск vs Обновление страницы

## Когда нужно перезапускать бота?

### ✅ Перезапуск НЕ нужен (достаточно обновить страницу F5):

- Изменения в `public/` (HTML, CSS, JavaScript)
  - `public/index.html`
  - `public/app.js`
  - `public/style.css`
- Изменения в веб-интерфейсе
- Тестирование через браузер

### ⚠️ Перезапуск НУЖЕН:

- Изменения в `src/` (TypeScript код сервера)
  - `src/index.ts`
  - `src/bot/handlers.ts`
  - `src/server/api.ts`
  - `src/db/*.ts`
  - `src/config/*.ts`
  - `src/ai/*.ts`
- Изменения в `.env` файле
- Установка новых npm пакетов
- Изменения в `package.json`

## Как перезапустить бота?

### В терминале:

1. Остановите текущий процесс: `Ctrl+C`
2. Запустите снова: `npm run dev`

### Или используйте автоперезагрузку:

Если бот запущен с `npm run dev` (tsx watch), он автоматически перезапустится при изменении файлов в `src/`.

## Текущая ситуация

**Вы изменили код в `src/`** → Нужен перезапуск бота!

После перезапуска:
1. Обновите страницу в браузере (F5)
2. Убедитесь, что Chat ID указан
3. Попробуйте тест снова

## Проверка работы

После перезапуска проверьте логи в терминале:

```
🚀 Запуск простого бота...
🔄 Initializing database...
✅ Database initialized successfully.
🧠 Контекстная память:
   Статус: ✅ Включена
   ...
```

Если видите эти логи - бот запущен правильно!
