# 🧪 Инструкция по тестированию контекстной памяти

## Проблема

Если бот не помнит предыдущие сообщения, это означает, что контекст не используется. Вот как это исправить:

## ✅ Правильный способ тестирования

### Шаг 1: Создайте тестовые данные

Запустите тестовый скрипт для создания истории сообщений:

```bash
npx tsx test-context-simple.js
```

Этот скрипт создаст:
- Chat ID: `test_simple_context`
- Сообщение пользователя: "Мой код доступа: 12345"
- Ответ бота: "Понял, запомнил!"

### Шаг 2: Проверьте в веб-интерфейсе

1. Откройте http://localhost:3000
2. Перейдите в секцию "🤖 Выбор AI модели"
3. Убедитесь, что выбрана модель (например, GPT-4)
4. В поле **"Chat ID"** введите: `test_simple_context`
5. В поле **"Введите сообщение для теста"** введите: `Какой код доступа я тебе только что назвал?`
6. Нажмите **"Протестировать"**

### Шаг 3: Проверьте результат

Вы должны увидеть:
- ✅ Ответ AI, который упоминает код доступа "12345"
- 📚 Сообщение: "Использован контекст из X сообщений"

## 🔍 Отладка

### Проблема: "Контекст не использован"

**Причины:**
1. Chat ID не указан в тесте
2. Контекстная память отключена в настройках
3. Нет истории сообщений для этого Chat ID

**Решение:**
1. Проверьте секцию "🧠 Контекстная память" - должна быть включена
2. Убедитесь, что указали правильный Chat ID
3. Создайте историю сообщений через тестовый скрипт

### Проблема: "История пуста для Chat ID"

**Причина:** Нет сообщений в БД для этого Chat ID

**Решение:**
1. Запустите `npx tsx test-context-simple.js` для создания тестовых данных
2. Или используйте Telegram бота для создания реальной истории
3. Проверьте историю в секции "📜 История сообщений"

### Проблема: "Контекстная память отключена"

**Решение:**
1. Перейдите в секцию "🧠 Контекстная память"
2. Установите галочку "Включить контекстную память"
3. Нажмите "💾 Сохранить настройки"

## 📊 Проверка через API

Вы также можете проверить контекст напрямую через API:

```bash
# Получить контекст для чата
curl http://localhost:3000/api/context/test_simple_context?message=Какой%20код%20доступа

# Тест AI с контекстом
curl -X POST http://localhost:3000/api/ai/test \
  -H "Content-Type: application/json" \
  -d '{"message": "Какой код доступа я назвал?", "chatId": "test_simple_context"}'
```

## 🎯 Примеры тестовых Chat ID

После запуска тестовых скриптов доступны:

- `test_simple_context` - простой тест с кодом доступа
- `test_context_chat_789` - полный тест с диалогом о Python
- `test_chat_456` - базовый тест из test-db.js

## 💡 Важные замечания

1. **Контекст работает только для существующих чатов** - сначала создайте историю
2. **Chat ID должен совпадать** - используйте тот же Chat ID, что и при создании истории
3. **Контекст ограничен настройками** - по умолчанию используется последние 10 сообщений
4. **Системный промпт** - может быть включен или отключен в настройках

## 🐛 Логи для отладки

Проверьте логи в консоли бота при тестировании:

```
🧪 Тест AI: chatId=test_simple_context, contextEnabled=true
📚 Найдено 2 сообщений в истории для чата test_simple_context
📚 Контекст загружен: 3 сообщений для чата test_simple_context
✅ Контекст подготовлен: 3 сообщений (2 без системного промпта)
```

Если видите эти логи, значит контекст загружается правильно!
