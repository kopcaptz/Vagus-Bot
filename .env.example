# Telegram Bot Token (опционально - получите у @BotFather)
# Если не указан, бот будет работать только через веб-интерфейс
# TELEGRAM_BOT_TOKEN=your_bot_token_here

# Включить Telegram бота (true/false). По умолчанию true при наличии токена.
# TELEGRAM_ENABLED=true

# Порт для веб-сервера
PORT=3000

# Хост для веб-сервера (0.0.0.0 для доступа из сети, localhost только локально)
HOST=0.0.0.0

# AI API ключи (опционально)
# OpenAI API ключ (для GPT-4 и GPT-3.5)
# OPENAI_API_KEY=your_openai_key_here

# Anthropic API ключ (для Claude)
# ANTHROPIC_API_KEY=your_anthropic_key_here

# Модель по умолчанию при первом запуске (бот работает сразу, без выбора в веб-интерфейсе)
# openai-gpt-4 = gpt-4o, openai-gpt-3.5, anthropic-claude, none
DEFAULT_MODEL=openai-gpt-4

# AI модели и лимиты
# AI_MODEL_OPENAI=gpt-4o
# AI_MODEL_ANTHROPIC=claude-3-5-sonnet-latest
# AI_MAX_TOKENS=4096
# AI_MAX_ITERATIONS=10
# AI_MAX_RETRIES=2

# Rate limiting (запросов в минуту на пользователя)
# RATE_LIMIT_MAX=20
# RATE_LIMIT_WINDOW_MS=60000

# Security (безопасность)
# Токен для доступа к веб-панели (если пустой — доступ без авторизации)
# ADMIN_TOKEN=my-secret-token-here

# Режим доступа к Telegram боту: open (все) или allowlist (только разрешённые)
# TELEGRAM_ACCESS_MODE=open

# Белый список Telegram (user_id или @username через запятую)
# TELEGRAM_ALLOWLIST=123456789,@myusername

# Drive (корень диска для DriveSkill; в Docker обычно /app/drive)
# DRIVE_ROOT=/app/drive

# Хост-путь для тома /app/drive (docker-compose). Должна быть РЕАЛЬНАЯ папка (не симлинк).
# По умолчанию ./drive. Для Google Диска: G:/Мой диск/Vagus-Bot (слэши вперёд).
# VAGUS_DRIVE_HOST=./drive
# VAGUS_DRIVE_HOST=G:/Мой диск/Vagus-Bot

# Function Calling (инструменты: read_file, write_file, run_command)
# По умолчанию false — инструменты выключены
# TOOLS_ENABLED=false

# Рабочая директория для файловых операций (относительно process.cwd)
# Пусто = файловые операции запрещены
# WORKSPACE_ROOT=./workspace

# Таймаут выполнения команд (мс)
# TOOL_COMMAND_TIMEOUT_MS=15000

# Long-term memory: data/memory/{userId}.md + семантический поиск (memory_search)
# Embeddings (опционально): по умолчанию используются OPENROUTER_API_KEY и BASE_URL
# EMBEDDINGS_BASE_URL=https://openrouter.ai/api/v1
# EMBEDDINGS_API_KEY=sk-or-v1-...
# EMBEDDINGS_MODEL=text-embedding-3-small
# EMBEDDINGS_TIMEOUT_MS=10000

# Code Interpreter (code_exec) — WASM-песочница, без настройки
# Web Reader (web_fetch) — чтение веб-страниц в Markdown, без настройки

# ─── CLI Gateway (system_cli_gateway) ───
# Безопасный шлюз для выполнения CLI-команд (shell-free, allowlist-based).
# Требует TOOLS_ENABLED=true для активации.

# Режим безопасности (задаётся ТОЛЬКО при старте, runtime-переключение отсутствует):
#   OFF     — (default) полная блокировка, инструмент не выполняет команд
#   SAFE    — только read-only команды (git status, git log, npm list, ...)
#   LIMITED — безопасные write-операции (git add, git commit, ...)
#   CONFIRM — опасные операции (git push, npm install, ...) требуют токена подтверждения
# CLI_GATEWAY_MODE=OFF

# Корневая директория проекта (sandbox для cwd; относительно process.cwd)
# CLI_GATEWAY_PROJECT_ROOT=./

# Env kill-switch: если = "1" — gateway мгновенно блокируется (режим OFF)
# Также можно создать файл STOP.flag в корне projectRoot для мгновенной блокировки.
# CLI_GATEWAY_KILL=

# (Опционально) Дополнительные доверенные директории для Trusted Path Resolver (через запятую)
# CLI_GATEWAY_TRUSTED_DIRS=D:\tools,E:\portable

# (Опционально) Override пути для бинарников, если установлены нестандартно
# CLI_GATEWAY_GIT_PATH=D:\PortableGit\bin\git.exe
# CLI_GATEWAY_NPM_PATH=D:\nodejs\npm.cmd
# CLI_GATEWAY_NODE_PATH=D:\nodejs\node.exe

# TTL токена подтверждения (мс, допустимо 120000–300000, по умолчанию 180000 = 3 мин)
# CLI_GATEWAY_CONFIRM_TTL=180000

# Таймаут выполнения команды (мс, минимум 5000, по умолчанию 15000)
# CLI_GATEWAY_TIMEOUT_MS=15000
